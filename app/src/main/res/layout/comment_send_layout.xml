<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:id="@+id/comment_box_frame"
    android:layout_gravity="bottom"
    android:layout_height="wrap_content">

    <EditText
        android:id="@+id/comment_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/no_border_edit_text_white"
        android:hint="@string/type_message"
        android:padding="@dimen/layout_margin"
        android:textSize="14sp" />

    <ImageButton
        android:id="@+id/add_comment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical|end"
        android:layout_marginEnd="@dimen/image_button"
        android:layout_marginRight="@dimen/image_button"
        android:background="?selectableItemBackgroundBorderless"
        android:padding="@dimen/image_button"
        android:src="@drawable/send" />

</FrameLayout>